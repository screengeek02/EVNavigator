# EVNavigator

This project contains a Next.js marketing experience for EVNavigator, a platform for planning electric vehicle journeys. Run the
development server with `npm install` followed by `npm run dev`, then open http://localhost:3000 to explore the refreshed landing
page.
